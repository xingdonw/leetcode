class Solution:
    def reverseWords(self, s: str) -> str:
        l = s.split(" ")            #先按照空格把每一段单词分出来
        for i in range(len(l)):
            l[i]=l[i][::-1]         #每个单词逆回来
        return " ".join(l)          #连在一起
